#include "main.h"
/**
*_islower _ short description, single line
* @c: contain value to be compared
*return: always 0.
*/
int_islower(int c)
{
  if (c >'a' && c< 'z')
  {
    return (1)
    }
  eles
  {
    return (0)
    }}
